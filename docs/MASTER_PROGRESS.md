# Website Workstream — Master Progress

**Check this file first, every session.** It is authoritative for status; the numbered docs are
authoritative for content.

**Opened:** 2026-07-31 · **Current state:** plan complete (docs 00–17) · **M0–M4 built: the full
~45-route site is live, building green, with zero broken internal links** · **Next action:** confirm
positioning (`01` §2), then M5 (analytics, Lighthouse CI, real screenshots).

**Scope change (2026-07-31, user):** the site is built **from scratch in `website/`**, not by
rebuilding `apps/marketing`. `website/` is now plan *and* code — a self-contained Next.js 16 app on
port 3500, outside the root npm workspace. `apps/marketing` is untouched.

**Theme decision (2026-07-31, user): light mode only.** No dark theme, no toggle. The dark bands on
§8 and §13 are a tonal device using `--color-band-*`, not a theme.

**Rebrand (2026-07-31, user): RocketCRM → Algoryq One, powered by Algoryq.tech.** The product is now
an **Algoryq Technologies** product and the site says so. Landed across all ~45 routes:

- **Mark** — the parent glyph from `algoryq.com`, copied path-for-path into
  `src/components/site/logo.tsx`; new `src/app/icon.svg` is a tonal inversion of the parent favicon
  so the two are never confused in a tab strip.
- **Wordmark** — `Algoryq` + `One`, second segment in brand blue, mirroring the parent's
  `algoryq` + `.tech` colour split.
- **Palette** — brand hue 277 (indigo) → **259 (Algoryq blue)**, with the 500/600 stops taken
  verbatim from the parent; neutrals re-tinted to the parent's ink hue 265. Contrast *improved*
  (primary action on white 5.32:1 → **7.71:1**); all 26 pairs still pass (`02` §2).
- **Identity** — `one.algoryq.com`, portal at `portal.one.algoryq.com`, contact addresses on
  `algoryq.com`. All of it lives in `SITE` / `PARENT` / `CONTACT` in `src/content/site.ts`.
- **Attribution** — footer carries "Powered by Algoryq.tech"; copyright names Algoryq Technologies.

Positioning was **not** touched: the wedge, ICP and objection map were never name-dependent (`01` §0).
Capability claims were not touched either — `00-audit-and-inventory.md` still describes the same code.
The binding rules are `CLAUDE.md` rule 12 and `01` §0.

## What is built

**47 routes, all building green.** `npm run build` succeeds with zero services running.

| Area | State |
|---|---|
| App scaffold (Next 16, Tailwind v4, TS strict, own lockfile, port 3500) | ✅ |
| Token system — light only, oklch, brand hue 259 (Algoryq blue) | ✅ `src/app/globals.css` |
| Content modules (typed, copy out of JSX) | ✅ 12 modules incl. per-page SEO map |
| Chrome — header w/ mega-menu + mobile sheet, footer, logo, skip link | ✅ |
| Homepage — 20 sections in narrative order | ✅ server-rendered |
| Product cluster pages ×5 | ✅ shared template, real modules/permissions/services |
| Platform pages ×6 | ✅ incl. an honest scope note on workflow triggers |
| Security hub + 3 deep pages | ✅ posture table with Implemented/In progress/Planned |
| Developer pages ×4 | ✅ |
| Solutions: index + 3 industries + 5 roles | ✅ |
| Pricing, ROI, FAQ, demo | ✅ live plan data with a degradation path built first |
| Company ×3, legal ×3, resources ×3 | ✅ |
| Interactive islands ×4 | ✅ cluster switcher, permission matrix, ROI calculator, contact form |
| Diagrams | ✅ three-systems + architecture, with text equivalents |
| SEO infra | ✅ sitemap from route manifests, robots (noindex unless `SITE_INDEXABLE`), canonicals, JSON-LD, security headers, legacy redirects |
| Verification scripts | ✅ `check:links` (crawler) + `check:content` (fabrication/vocab/headings/metadata/alt) |

### Verified this session

| Check | Result |
|---|---|
| `tsc --noEmit` strict | Clean |
| `npm run build` | Clean, 48 routes |
| Build with all service URLs unreachable | **Green** — the tolerance requirement holds |
| Internal link crawl | **44 URLs, zero broken**; `/signup` and `/login` 307 to the portal as designed |
| Content guard over 42 routes | **Zero failures, zero warnings** |
| Contrast, by arithmetic | **26/26 token pairs meet target** |
| **Browser audit: 43 routes × 13 viewports = 559 page loads** | **No findings.** Zero horizontal overflow, zero serious/critical axe violations, zero undersized tap targets, zero sub-12px content text, zero console errors — at every width from 320px to 2560px, including landscape phone |
| Degradation paths, live | Pricing → quote path (no invented prices); blog + changelog → honest empty states; contact → fallback email, not a fake success |
| Screenshots | 52 captured across 4 routes × 13 viewports (`audit-shots/`, gitignored) |

### Not yet done

Lighthouse CI, bundle budgets, a **manual screen-reader pass** (automated tooling catches roughly a
third of real issues — this is the largest remaining accessibility gap and is disclosed on
`/legal/accessibility`), forced-colors mode, `eslint-plugin-jsx-a11y`, real product screenshots
(needs the platform booted — doc 10 §4), the full five-tab product tour from `04` §6, comparison
pages (`/compare/*`, needs legal review), and `/customers`.

---

## Documents

| # | Document | Purpose | Status |
|---|---|---|---|
| — | [CLAUDE.md](../CLAUDE.md) | Operating guide, working rules, input list | ✅ Written |
| 00 | [Audit & Inventory](00-audit-and-inventory.md) | What Algoryq One actually is, verified against code | ✅ Written · Figma sub-task blocked |
| 01 | [Brand & Positioning](01-brand-and-positioning.md) | Wedge, ICP, voice, narrative spine, objection map | ✅ Written · needs GTM sign-off |
| 02 | [Design System](02-design-system.md) | Tokens, type, color, space, elevation, motion, components | ✅ Written |
| 03 | [Information Architecture](03-information-architecture.md) | Sitemap, nav, 37→20 section resolution, URL contract | ✅ Written |
| 04 | [Homepage Blueprint](04-homepage-blueprint.md) | All 20 sections, spec'd to build level | ✅ Written |
| 05 | [Module Showcase Pages](05-module-showcase-pages.md) | 5 cluster + 6 platform pages | ✅ Written |
| 06 | [Solutions & Industries](06-solutions-and-industries.md) | Industry, role, comparison, customers | ✅ Written |
| 07 | [Security, Trust & Compliance](07-security-trust-compliance.md) | Trust centre + the honesty contract | ✅ Written · needs security + legal sign-off |
| 08 | [Pricing, ROI & Conversion](08-pricing-roi-and-conversion.md) | Money pages + conversion discipline | ✅ Written |
| 09 | [Motion & Interaction](09-motion-and-interaction.md) | Choreography system, reduced motion | ✅ Written |
| 10 | [Visual Assets & Devices](10-visual-assets-and-devices.md) | Screenshot pipeline, device showcase, diagrams | ✅ Written |
| 11 | [Frontend Architecture](11-frontend-architecture.md) | Stack, structure, rendering, budgets, CI | ✅ Written |
| 12 | [Performance & SEO](12-performance-and-seo.md) | Budgets, CWV, structured data | ✅ Written |
| 13 | [Accessibility](13-accessibility.md) | WCAG 2.2 AA, testing matrix, responsive matrix | ✅ Written |
| 14 | [Figma Deliverables](14-figma-deliverables.md) | Design-file output, sync direction | ✅ Written · **blocked on file URL** |
| 15 | [Content & Copy System](15-content-and-copy-system.md) | Copy standard, content model, review | ✅ Written |
| 16 | [Analytics & Experimentation](16-analytics-and-experimentation.md) | Events, funnel, A/B, privacy | ✅ Written |
| 17 | [Implementation Roadmap](17-implementation-roadmap.md) | Milestones, effort, risks, DoD | ✅ Written |
| — | [Palette Migration](palette-migration.md) | 8 hues → 3 (Ink · Blue · Sand): blast radius, every replacement decision, quarantine audit | ✅ Landed 2026-08-08 |
| — | [Spacing & Content Audit](spacing-content-audit.md) | Section rhythm + content-reduction findings | ✅ **Phase 2 landed 2026-08-10** — all 12 work-plan items, 4 documented deviations |
| — | [Homepage Section Reduction](homepage-section-reduction-proposal.md) | 16 sections → 9: which merge, where every claim survives, 4 costed options | 🟡 **Proposal written, awaiting approval. No code.** |

---

## Milestones

| Milestone | Docs | Status | Exit criterion |
|---|---|---|---|
| M0 Foundations | 02, 11 | 🟢 Built | Tokens, primitives, chrome, content modules, green build. Storybook outstanding. |
| M1 Homepage | 04, 09, 10, 15 | 🟢 Built | All 20 sections live. Outstanding: screenshots, `04` §6 full product tour. |
| M2 Trust & conversion | 07, 08 | 🟢 Built | Security hub + 3 deep pages, pricing, ROI, demo, contact. A CISO and a CFO can both self-serve. |
| M3 Product depth | 05, 10 | 🟢 Built | 5 cluster + 6 platform pages on one template. Screenshots outstanding. |
| M4 Market surfaces | 06, 15 | 🟢 Built | Solutions, developers, resources, company, legal. `/compare/*` and `/customers` deferred — both need input we do not have. |
| M5 Measure & tune | 12, 16 | ⬜ Not started | Funnel instrumented, leaks visible |
| M6 Design artifacts | 14 | ⬜ Not started | Figma Foundations + Components generated |

---

## Blockers & open inputs

| # | Item | Blocks | Owner | Status |
|---|---|---|---|---|
| B1 | ~~No Figma file URL~~ | Doc 14 | — | ✅ **Closed 2026-07-31.** File supplied and analysed: a product-story deck, not page designs. Brand mark, accent bar, four-app framing and the canonical demo dataset adopted; "24 services" reconciled to 30 in favour of code. |
| B2 | **No customer logos or written permission.** | Homepage §2 logo state, `/customers` | Business | 🟡 Open — engineering-proof fallback ships regardless |
| B3 | **No testimonials or case studies.** | Homepage §15, `/customers` | Business | 🟡 Open — sections gated, never faked |
| B4 | **No SOC 2 / ISO certification.** Architecture is ready; no audit performed. | Compliance badges | Business | 🟡 Open — "ready architecture" language only; badge rule is absolute |
| B5 | **No legally approved uptime SLA.** | Any % availability claim | Legal | 🟡 Open — architectural claims only |
| B6 | ~~Positioning wedge not confirmed~~ | Everything downstream of doc 01 | — | ✅ **Closed 2026-07-31.** "Deal → delivery → cash" chosen. Doc 01, the homepage copy, the section order and the site metadata were rewritten to match; a new `Chain` centrepiece section was built. The three unchosen options and the previous wedge are retained as A/B variants in `01` §9. |
| B7 | **Font licence undecided** (Inter-only vs licensed display face). | M0 | Design/Business | 🟠 Open — Inter-only fallback is specified and acceptable |
| B8 | **Pricing model decision** (public tiers vs contact-sales). | `/pricing` copy | Business | 🟡 Open — live plan data works either way |
| B9 | **Security + legal sign-off process** for `/security` and `/compare` not yet established. | M2, M4 | Security/Legal | 🟠 Open |
| B10 | **The hero video's baked-in text is misspelled throughout — not just the domain.** Flagged by the client, then **verified visually 2026-08-10** by rendering the homepage at 1728px, 2× DPI, and reading the frame (`audit-shots/home-1440.png`; a 2× crop was taken to confirm each string). It cannot be verified from source — the text is in the video's pixels, not in any file in this repo. `public/media/hero-loop.mp4` / `.webm` shows: <br>• URL bar **`partal.one.algerys.con/actions`** — *three* errors against the real host `portal.one.algoryq.com` (`SITE.appUrl`): `partal`→`portal`, `algerys`→`algoryq`, `con`→`com`. **The brand name itself is misspelled.** <br>• **`Orhit Health`** → should be `Orbit Health` <br>• **`Awalling finance approvai`** → should be `Awaiting finance approval` (two errors) <br>• **`Alies CRM Roliout`** → should be `Atlas CRM Rollout` (two errors) <br>The correct strings are all in `heroVisual.portal` in `content/homepage.ts`; the render did not use them. This reads as machine-generated placeholder text, and it is the **first thing a visitor sees** on the homepage — above the fold, at every width from 1024px up. | Homepage §1 hero credibility. Nothing functional. | **Whoever owns the video asset.** `hero-loop.mp4` is a generated render with no project file in this repo, so it must be re-exported at source with the text corrected against `heroVisual.portal`. | 🟠 **Open — needs a re-cut asset; no code fix is possible.** Interim options: (a) ship as-is; (b) **revert the hero to `components/diagrams/integration-web.tsx`** — the DOM/SVG composition the video replaced on 2026-08-09, which renders `heroVisual.portal.url` and the feed rows from `content/homepage.ts`, so it is always correct, always spelled right, resolution-independent and ~0 bytes; (c) re-frame or crop the video to exclude the browser chrome, if the composition survives it. **Recommend (b) until a corrected render exists.** The diagram was deliberately kept in the repo for exactly this case — see the comment at the top of `sections/hero.tsx`. |

| B11 | **The new Reporting screenshot has fabricated-looking data errors baked into the pixels.** `public/images/reporting-utilisation-dashboard.jpg`, placed on the homepage §7 Reporting section 2026-08-10 at the client's request. Verified by cropping the source at 1:1 (three regions, `audit-shots`-style): <br>• **X-axis reads `Jan 1 · Jan 13 · Jan 9 · Jan 16 · Jan 22 · Jan 25 · Jan 27 · Jan 29 · Jan 33`** — `Jan 33` is not a date, and the sequence is out of order (13 before 9). <br>• **Chart legend reads `Capacity (iri hours)`** — garbled; presumably meant `(160 hours)`. <br>• **The member table repeats one identical row five times** — `Sarah Jensen · Dev · Alpha Project · 150 · 160 · 94%`. <br>• The names are **not the canonical Demo tenant dataset** (`content/demo-tenant.ts` uses Ava Kapoor, Atlas CRM Rollout, etc.), so the standard "Sample data from the Algoryq One Demo tenant" caption would have been false. The caption was written as "Illustration of the reporting surface. Sample data." instead. <br>**Why it matters:** this is the same class of defect as B10 and it sits in the section arguing that *"utilisation is a query, not a reconstruction"*. A dashboard illustrating trustworthy numbers should not show an impossible date and five identical rows. Rule 1 forbids fabricated data; a labelled *illustration* is permitted, but one with visible errors reads as carelessness on a site whose whole pitch is that its numbers can be defended. <br>**One thing is right:** the URL bar reads `portal.one.algoryq.com/reports` — correctly spelled, unlike B10. | Homepage §7 credibility | **Whoever owns the image asset** — it is a generated mockup, so it needs re-rendering with real Demo-tenant values, a valid date axis, a readable legend and distinct table rows | 🟠 **Open — needs a re-rendered asset.** Options: (a) ship as-is; (b) re-render from `content/demo-tenant.ts` values; (c) crop the table and axis out and show only the chart header + saved-view controls, which are error-free. **Recommend (b).** Separately and cosmetically: the image carries its own **mint-green mat** around the browser card, which clashes with the section's `--color-cyan-50` tint — a re-export should either drop the mat or match the section surface. |

| B12 | **Half the subsection illustration set has garbled micro-text baked in.** The 18 files in `public/illustrations/`, placed 2026-08-10. Each was opened at 1:1 before placement. **Nine are clean** — `home`, `product-revenue`, `platform-architecture`, `platform-ai`, `security`, `security-permissions`, `security-compliance`, `security-infrastructure`, `pricing`; several are genuinely accurate (`security.jpg` names the six real control areas, `security-permissions.jpg` states the real resolution order, `pricing.jpg` names the six real clusters). **Nine carry defects in their small type:** <br>• `product-delivery` — "Blanding Design", "Prodectt Design", "Featuree Inagnut" <br>• `product-people` — "Languag", "SAII", "Excent", "Secturent"; leave calendar skips 12–13 <br>• `product-intelligence` — "Select ton", "Data nsknvra", "Select usags" <br>• `platform-customization` — "Email fore" <br>• `platform-workflows` — "Final Reve" truncated, **one completely empty node**, and the "Request Submitted → Manager Approval" pair duplicated <br>• `platform-mobile` — "Planert/Plansst/Planst/Plarest project"; weekday dates out of order (Sat 17 first, Fri 15 after Thu 18) <br>• `platform-white-label` — "Buildert your brand enterprise SaaS, platform"; the live-preview pane is **violet**, a hue the palette migration deliberately removed <br>• `developers` — status labels truncated to "Suc" and "Not" <br>• `product-service` — the SLA countdown badge is duplicated <br>**Why it matters:** same class as B10 and B11 — text baked into pixels that no code change can fix, on a site that argues its numbers can be defended. At the rendered size (408–830px wide) most of this is not legible, which is the only reason it is 🟠 and not 🔴; it becomes obvious the moment anyone opens an image or views on a high-DPI screen. | Visual credibility on 14 routes | **Whoever owns the illustration set** — these are generated mockups and need re-rendering with correct label text | 🟠 **Open.** Options: (a) ship as-is, since the defects are sub-legible at render size; (b) re-render the nine with corrected labels; (c) blur or crop the offending regions. **Recommend (b).** Also worth noting for a re-render: the whole set sits on a **mint-green background** that clashes with the site's `--color-cyan-50` tint sections, and `platform-customization.jpg` has its own window chrome painted in — the `Illustration` component takes `chrome={false}` for that one so the traffic lights are not stacked twice. |

Legend: 🔴 hard blocker · 🟠 needs a decision soon · 🟡 has a working fallback

---

## Decisions on record

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-31 | ~~Positioning wedge = "one permission model, three departments"~~ — **superseded same day** | See row below |
| 2026-07-31 | **Positioning wedge = "deal → delivery → cash"** (chosen by the user from four costed options) | The operational chain is concrete, demonstrable in one screen-share, and closest to the money — it speaks to the COO/CFO who signs. The permission model is demoted from headline to *proof layer*: it is the deepest moat but a governance argument, and governance wins the CISO rather than the budget holder. Accepted cost: this puts us in the PSA category against Kantata/Certinia rather than against Salesforce, and the HR modules become supporting cast (`01` §3). |
| 2026-07-31 | Homepage = **20 sections**; the other 21 requested topics become destination pages | 37 sections contradicts the keynote requirement; nothing is dropped (`03` §1) |
| 2026-07-31 | Site keeps a **standalone theme**, does not import `@algoryq/ui` | Different job, bundle weight, release independence (`02` §1) |
| 2026-07-31 | **Built from scratch in `website/`**, `apps/marketing` untouched | User instruction; also gives a self-contained install with no monorepo coupling |
| 2026-07-31 | **Light mode only.** Dark bands are a tonal device, not a theme | User instruction; one theme designed properly beats two designed adequately |
| 2026-07-31 | **Code is the source of truth; Figma is a generated output** | Confirmed on inspection — the supplied file already understated the service count (`14` §1) |
| 2026-07-31 | **Canonical demo data comes from Figma**, replacing the invented "Northwind" fixture | One dataset shared by site, deck and screenshots (`10` §2, `14` §1) |
| 2026-07-31 | Hero visual is **DOM/CSS, not a screenshot** | Crisp at any DPI, ~0 bytes, and cannot go stale when the product ships a release. Real screenshots land in §6 and the module pages. |
| 2026-07-31 | **Content in typed repo modules, no CMS** | Type safety, code review on copy, zero runtime cost (`11` §5) |
| 2026-07-31 | Trust content is **gated on real data, never fabricated** | Working rule #1; a fake SOC 2 badge is a legal problem, not a design choice |
| 2026-07-31 | Lighthouse Performance gated at **≥97 mobile / ≥99 desktop** (median of 3), not a hard 100 | Lab-score variance makes a hard 100 gate flaky; field CWV is the real metric (`12` §1) |
| 2026-07-31 | Interactive demo (§6) uses **fixture data, labelled "Sample data"**, no backend calls | Honesty + performance + no coupling to service availability |
| 2026-07-31 | **No native mobile app claim** — responsive web + PWA only | It's what exists (`00` §7) |
| 2026-07-31 | **Renamed RocketCRM → Algoryq One**, branded as an Algoryq Technologies product | User instruction. The parent is a named engineering company, and for a buyer committing to a 30-service platform "who operates this in five years" is a real objection — an explicit parent answers it better than a standalone product brand (`01` §0) |
| 2026-07-31 | Site domain = **`one.algoryq.com`** (product subdomain), chosen by the user over `algoryqone.com` and `algoryq.one` | Keeps the endorsement in the URL itself, so the powered-by line reinforces rather than introduces the relationship |
| 2026-07-31 | Wordmark = **"Algoryq One", two words**, chosen by the user over the tight `algoryqone` lockup | Reads unambiguously aloud and in prose; the colour split still mirrors the parent's `algoryq`+`.tech` |
| 2026-07-31 | The parent glyph is **copied path-for-path, never redrawn** | Two brands sharing a mark must share it exactly, or they read as a knock-off at small sizes (`CLAUDE.md` rule 12) |
| 2026-07-31 | Brand hue 277 → **259**, with Algoryq's own 500/600 values used verbatim | A swatch lifted from either site must match. Contrast improved rather than regressed (`02` §2.1) |
| 2026-07-31 | `check-contrast.mjs` now **parses `globals.css`** instead of mirroring it by hand | The mirrored table passed all 26 pairs against tokens that had just been deleted. A checker that can pass against colours the site no longer ships is worse than none (`02` §2.3) |
| 2026-08-09 | **Palette collapsed to 3: Ink · Blue · Aurora Cyan** (client-approved; supersedes the sand direction below, same day). `--color-accent` promoted from a single value to a full 7-step **cyan ramp** (hue 200); `--color-info` and `--color-bg-warm` deleted — warm parchment would have been a fourth hue. `--color-app-customer` moved onto cyan-600, `--color-app-platform` off violet onto ink. `Section tone="warm"` renamed **`tone="tint"`** and repointed at `--color-cyan-50`. Blue and Ink untouched (rule 12 holds). **The load-bearing part is the cyan contrast contract:** cyan-500 `#00BEC7` is **2.29:1 on white** and fails both the 4.5:1 text and 3:1 non-text thresholds — the old comment on `--color-accent` claimed it "holds 3:1 as a non-text accent on white", which was never measured and is false. Cyan-500 is now large-decorative-fill-only with ink on top (7.74:1); text uses cyan-700 (5.11:1), borders and icons cyan-600 (3.35:1), the dark band cyan-300 (12.08:1). **Why it matters:** a single "accent" token with an unverified contrast claim silently authorised an unreadable colour anywhere a designer reached for it. Splitting one value into role-specific steps makes the correct choice the easy one, and `check:contrast` now enforces it — including a `FILL_ONLY` assertion that fails if cyan-500 ever stops being fill-only. Full record: `palette-migration.md`. |
| 2026-08-08 | ~~**Palette collapsed from 8 non-semantic hues to 3: Ink · Blue · Sand**~~ — **superseded 2026-08-09**, see the row above. Implemented and fully verified before being reversed; the sand ramp, the warm aurora radial and the sand favicon crossbar were all reverted. What survived the reversal: the semantic-colour quarantine, the two decorative `--color-danger` removals, the `CtaBand` double-border fix, and the section-rhythm structure (which only changed surface, not shape). (client-approved). `--color-accent` (cyan, hue 200) and `--color-info` (hue 250, zero usages) deleted; `--color-bg-warm` promoted to a real 6-step **sand ramp** (hue 85); the four app-identity colours re-hued off cyan and violet onto blue + ink. Blue and Ink are untouched — the brand ramp (259/260) and neutral ramp (265) keep every value, so rule 12 still holds. Sand is a **surface family**: never text, never an accent. The four semantic colours (success, success-band, warning, danger) survive but are **quarantined to functional use only** — form validation, error/empty states, destructive confirmations and genuine status indicators. Two decorative usages were removed (a red rule beside a marketing bullet, a red dashed box around the competitor diagram); `--color-success` marking a settled step in the chain diagram and hero wires is exempt as product semantics. **Why:** eight competing hues made every surface negotiable, so nothing read as deliberate — and a semantic red used for marketing tone means a real error has no colour left that means "error". Section rhythm was rebuilt on sand at the same time, since the flatness and the hue sprawl were the same problem: the page alternated white against one cool grey and every section read identically. Full blast-radius inventory and every replacement decision: `palette-migration.md`. |
| 2026-08-09 | `check-contrast.mjs` extended from 26 to **48 pairs + a `FILL_ONLY` mode** | The checker tests a fixed list, so a colour not in the list is not verified. It earned this twice in one day: `brand-400` measured 2.67:1 as an app dot, and `cyan-100` measured **1.09:1** as the tint section border — fainter than the neutral border it replaced, i.e. an invisible section boundary. Both were caught by arithmetic, not by eye. The new `FILL_ONLY` mode asserts *both* halves of a fill-only colour: that ink on it passes, **and** that it stays below 3:1 on white — so "fixing" cyan-500 fails the build and forces a deliberate decision rather than silently retiring the rule |
| 2026-07-31 | Off-site routes (`/signup`, `/login`) render as **plain `<a>`, never `<Link>`** | `<Link>` prefetches, which follows the config redirect cross-origin to the portal. The old placeholder host answered so this stayed invisible; `portal.one.algoryq.com` is not provisioned yet, and the browser audit surfaced it as 715 findings — one failed prefetch on every route at every viewport. Prefetching a host we are about to redirect to is wasted work regardless. Kept as `OFFSITE_ROUTES` in `src/content/site.ts`, in sync with `next.config.mjs` |

---

## Change log

| Date | Change |
|---|---|
| 2026-09-02 | **The azure dark ground is applied. CLAUDE.md rule 10 rewritten; 48/48 contrast pairs pass; zero rendered text below threshold across 14 routes.** <br>**Rule 10 changed first, because rule 10 required it** ("do not reintroduce a dark variant without changing this rule first"). Light-only stood 2026-07-31 → 2026-09-02 and is recorded rather than deleted, because a great many comments in this repo were written under it. <br>**No new palette.** The ground was sampled from the M6 deck — its slides read `#030a1b`–`#050d21` with `#dfecfa` text, which lands within a hair of `--color-neutral-950`. So this was a re-point of the existing ink ramp read from its dark end. No hue moved and rule 12 is untouched. <br>**The semantic layer was the actual work, and the reason a six-line flip does not work.** 26 component files referenced ramp steps *directly* for text — `--color-brand-700` links, `--color-brand-800` chip labels, `--color-cyan-700` accents — all 2–3:1 on the ground. ~70 call sites across 20 files migrated to `--color-action` / `--color-action-hover` / `--color-link` / `--color-link-strong` / `--color-chip*` / `--color-tint*` / `--color-accent-*`. Order was semantic layer → call sites → surfaces → contrast gate; any other order ships a site failing its own gate on all 48 routes. <br>**Five decisions taken where the inversion was not a straight flip:** (1) **`--color-fg-inverse` stays light.** The name invites the opposite and I set it dark first; the gate caught it at 2.5:1 immediately. The primary action is still a *dark* blue, so text on it is light on either ground. (2) **The band device inverted rather than being deleted** — on a dark ground a dark band is invisible, so `--color-band` is now a *raised* panel. Re-pointing one token kept every `tone="band"` call site and all its `band-fg` text working untouched, instead of editing them all to `tone="subtle"`. (3) **App identity dots re-derived from the light end** — `app-portal` at 0.275 L measured **1.23:1** on the dark surface, an invisible dot. The two-dimensional separation that makes four legible (lightness / chroma / hue) is preserved, mirrored. (4) **The cyan fill-only rule is retired and its premise is what changed** — cyan-500 was 2.29:1 on white and therefore fill-only; on the ground it is 8.5:1 and is a legitimate border and icon colour. The trap flipped rather than vanishing: text on a cyan fill must now be dark, and no such pair is asserted because nothing renders one. (5) **`success` and `danger` read from the light end** (0.53→0.75, 0.58→0.70); `warning` needed no change, and this incidentally clears the long-standing `/security` "In progress" pill that failed at 2.29:1 on the old light tint. <br>**`check:contrast` rewritten, not bypassed.** 14 pairs re-pointed from ramp steps to semantic roles, `fg-inverse` added, four stale light-ground pairs replaced, `FILL_ONLY` emptied with the reasoning recorded in the script. **48/48 pass.** Measured highlights: `link on bg` 11.14:1, `accent-text on surface` 11.50:1, `accent-line on bg` 9.97:1, `cyan-300 on band` 10.39:1. <br>**Verified beyond the token pairs, which is the part that matters.** A browser sweep resolved every rendered text node's computed colour against its real composited ancestor background (canvas-resolved, because Chrome returns `lab()` for oklch and a naive regex reads those numbers as RGB — my first probe did exactly that and reported 14 phantom failures). Result: **0 text nodes below threshold across 14 routes** — `/`, `/pricing`, `/security`, `/product/revenue`, `/platform/architecture`, `/security/permissions`, `/solutions`, `/solutions/agencies`, `/developers/api`, `/resources/guides`, `/company/about`, `/faq`, `/roi`, `/demo`. One real defect found and fixed: the `SystemArchVideo` fallback `<p>` was black-on-black, visible only if the video failed. <br>Also corrected: `globals.css`'s header still declared LIGHT MODE ONLY, §2.5's cyan rules described the white ground, and a comment in `intelligence.tsx` still said "No halo" after the halo shipped. Docs `02` §2 and §2.5 rewritten. <br>Verified: typecheck, build (48 routes), 48/48 contrast, 42 content routes, 48 links, rendered-contrast sweep clean. Axe gate running. |
| 2026-09-02 | **Parallax shipped — 6 of 6 patterns now built.** The deck's last outstanding pattern (slide 07: "Parallax is specified, not shipped", `STATUS not built`). Applied to the hero backdrop in `interactive/hero-video.tsx`: the layer moves **against** the pointer, capped at the specified **±8px**. <br>**Gated on pointer capability, not width** — `(hover: hover) and (pointer: fine)`. A touchscreen laptop is wide and must still be excluded, which a breakpoint query would not catch; the deck says "never on touch" and that is what enforces it. Reduced motion opts out entirely: this is decorative depth, exactly what the preference exists to switch off. <br>Two implementation notes. `scale(1.02)` on the layer is load-bearing, not decoration: without ~19px of overscan an 8px translate drags a bare edge into view on the opposite side. And the handler is rAF-throttled writing two custom properties — `pointermove` fires far faster than the compositor needs, and a style write per event is how this pattern ends up janky. <br>Measured: `-7.56px/+6.22px` at top-right and exactly inverted at bottom-left (so it moves against the pointer), every value within ±8px, `(hover: hover) and (pointer: fine)` false in a touch context so the listener never attaches, and no movement at all under reduced motion. <br>**Also fixed: an incremental-build artefact that looked like a broken link.** `check:links` reported a 404 on a `/_next/*.css` chunk — prerendered HTML from a previous build still referencing the old hash. A clean `rm -rf` rebuild resolved it: 48 URLs, none broken. Worth knowing that `NEXT_DIST_DIR` reuse can leave stale route HTML behind. |
| 2026-09-02 | **P3 built, and the motion system extended site-wide — all 7 beats now choreographed.** <br>**P3 Problem — the `timeupdate` driver, which is the deck's one correctness requirement.** The four gap lines are revealed off the video element's own clock, 500ms apart, in `interactive/gap-callouts.tsx`. A timer chain drifts the moment the video buffers, stalls, or is backgrounded (browsers throttle timers to ~1Hz there), and the labels then describe a frame that is no longer on screen; `currentTime` cannot drift because it *is* the position. Measured: at `t=0.9s` two lines shown, at `t=3.51s` all four. <br>**The callouts stayed beside the video rather than on it, deliberately.** The deck overlays them, but this section already names the same four gaps as text in the adjacent column — overlaying would state the section's one idea twice *simultaneously*, a stronger form of the duplication that removed `home.jpg`. Moving the list onto the footage would instead put body copy over moving video and forfeit its contrast guarantee. So the redline that transfers is the driver, implemented literally. <br>**Three failure paths, all closed:** the server renders every line (verified: 4 present in SSR), so no-JS is complete; reduced motion shows all and attaches nothing; and a failsafe reveals everything if the video never reaches the last cue — blocked autoplay, a stall, a decode error. The component starts at "all shown" and only counts up from zero once it has confirmed a video to read, so no path leaves copy hidden. <br>**Site-wide extension.** The stagger pattern was homepage-only; it now runs through `page-template.tsx`, which renders **~40 routes** — PageHero's jobs grid and RelatedLinks both got one observer and per-item delays. Plus the bespoke grids on `/solutions`, `/resources/guides` and `/pricing`. Verified across ten routes: every shared-template page shows 6 staggered items at 0/70/140ms, `/solutions` 8, `/resources/guides` 4, homepage 38. `/pricing` shows 0 only because the billing service is unreachable and the degradation path renders the quote block instead of plan cards — correct behaviour, not a miss. <br>Verified: typecheck, build (48 routes), 48/48 contrast, 42 content routes, 48 links. |
| 2026-09-02 | **P6 and P7 unblocked by building their compositions as DOM — which also cleared two of the three baked-in asset defects.** The deck called both OPEN with the reason stated exactly right: *"the composition is missing, not the technique."* Building the markup made every redline applicable and fixed the content at the same time. <br>**P6 Developers — the code panel is markup again.** It went inline `<pre>` → video (2026-08-09) → DOM (today), and the round trip is the point: P6 redlines `CHROME rise 320ms · ROWS 220ms · 180ms apart`, and an mp4 has no rows. Built server-side with no island and no framer. **It fixes the wrong environment variable**: the video typed `process.env.ALGORYQ_ONE_ONE_KEY` where the real key is `ALGORYQ_ONE_KEY` — a copy-pasteable error in the developer-facing section, and the worst of the three asset defects for that reason. The code is now selectable, searchable, copyable and readable by assistive technology, and a 4.1MB video leaves the section. **Deviation, stated:** rows are capped at `STAGGER_CAP`. The sample is 15 lines and 180ms × 15 is a 2.7-second entrance, against docs/09 principle 3 ("nothing waits on an animation"); capped, it completes in ~1.1s. The deck's own mock shows five rows, so the literal reading was never tested against a real sample. Still no status line — `invoice.created ok 142ms` is an invented latency (rule 1). <br>**P7 Intelligence — the chart is data.** Six bars `scaleY 320ms · 70ms apart`, exactly one haloed KPI at 320ms, annotation cross-fading at 200ms. Height comes from an inline custom property and the animation only scales what is already sized, so no layout property animates and the axis never moves. The halo is the CSS radial-gradient decided on 2026-09-01, reading `--color-cyan-500` directly. **"One KPI only" is enforced by the data** — the peak bar derives from `chart.peakWeek` rather than being marked up by hand, so it cannot drift into two. <br>**This is what retires blocker B11.** The screenshot's x-axis read Jan 1, 13, 9, 16, 22, 25, 27, 29, **33** — out of sequence, with a date that does not exist — and its table repeated one member five times. None of it was fixable from a content module while it was pixels. As data the weeks run in order and every figure is checkable arithmetic (`logged ÷ capacity`: 118/160 = 74%, 152/160 = 95%). Still fictional, still labelled sample (rule 1) — the difference is that it is now fictional *and coherent*. <br>Both retired assets stay in the repo unreferenced, as `GraphVideo` was: `TypeScript_code_typing_animation…mp4` and `reporting-utilisation-dashboard.jpg`. <br>**Not built, and it is the last one: P3's video overlays.** Unlike P6 and P7 there is no content to build it from — the four gaps are already named as text beside the video, so overlaying them states the section's one idea twice simultaneously, and inventing new callout copy to satisfy a motion spec is the wrong direction. B10 (`partal.one.algerys.con` in the §4 loop) is likewise untouched: that asset is a video whose composition §4 still wants. <br>Verified: typecheck, build (48 routes), 48/48 contrast, 42 content routes, 48 links. P6 measured 15 rows at `0.22s`, delays 0/180/360/540/720/900 then capped, env var correct, video absent. P7 measured 6 bars `bar-grow 0.32s` at 0/70/140/210/280/350, **halo count exactly 1**, annotation `0.2s`, raster absent. Axe gate running. |
| 2026-09-02 | **M6 "all phases" swept — the remaining inherit-only beats closed; the rest were already built or are still blocked on composition.** Working through the M6 brief's six phases against actual state rather than its assumptions. <br>**Phases 1–2 were already complete** (tokens, `motion.ts`, Reveal at 0.25, and P2 Chain / P4 Platform / P5 Capabilities) — which is what deck slide 09 says too, marking all three **BUILT** and `PATTERNS 6 of 6 built`. <br>**The brief renumbered the beats and got it wrong.** It lists P2 Capabilities, P3 Platform, **P6 Permissions**. Deck slide 06 is headed `P4 · P5 · SWITCHES AND SURFACES` and slide 09's table reads P2 Chain / P4 Platform / P5 Capabilities / P3·P6·P7 open — i.e. M6 keeps M5's numbering exactly, and **Permissions has no choreography slide in either deck**. So there is no deck-specified Permissions cross-fade; the brief invented that mapping along with the numbering. <br>**Built today, both inherit-only beats that had been skipped:** Permissions' two points and Final CTA's four blocks now stagger through `stagger()`. Final CTA is the deck's "button rises with delay" read honestly — headline, sub, buttons and tertiary link at 0/70/140/210ms, so the ask lands as an ask rather than a slab. Neither added an observer: both reused the `<Reveal>` already wrapping them. <br>**Homepage stagger is now complete and uniform** — seven groups, 37 items, all inside the 60–80ms band, measured: Problem 0/70/140/210 and 0/70/140 · Platform 0/70/140/210 and 0/70/140/210/280/350/350/350 (cap biting on the eight spine chips) · Permissions 0/70 · Capabilities 0/80/160/240 · Intelligence 0/70/140/210/280 · Security 0/70/140 · Final CTA 0/70/140/210. **32 observers**, unchanged from the Stage 2 conversion. <br>**Still not built, unchanged and for the same reason:** P3 overlays, P6 code rows, P7 KPI bars — deck slide 09 calls these OPEN and says it exactly right, *"the composition is missing, not the technique"*. Parallax likewise (slide 07, `STATUS not built`), and its target is the hero composition, whose layered form stopped existing at the 2026-08-31 full-bleed rebuild. <br>Verified: typecheck, build (48 routes), 48/48 contrast, 42 content routes, 48 links, reduced motion 0/1039 faded with every animation `none`. Axe gate re-queued against this build. |
| 2026-09-02 | **M6 Azure deck analysed — it is largely downstream of this repo's own reports, plus one genuinely new direction.** `Motion Reference M6 Azure.mp4` (1280×720, 30fps, **42s, 12 slides**, in `~/Downloads`). Read slide by slide, same method as M5. <br>**Slide 09 "HONESTY — Built, and only built", tagged MEASURED, contains this workstream's own verification numbers verbatim:** P2 Chain BUILT — *"token 0px from the last node at 1280 → 2560"*; P4 Platform BUILT — *"one observer on the grid · homepage 39 → 32"*; P5 Capabilities BUILT — *"rule 1344px, origin left"*; P3/P6/P7 OPEN — *"the composition is missing, not the technique"*. Footer: **`PATTERNS 6 of 6 built`**, **`BEATS 3 of 7 choreographed`**, **`BLOCKED ON composition, not motion`**. Those are the measurements and the phrasing from the 2026-09-01 entries below. So M6 is not new upstream instruction for the built work — it is a restatement of reported state, and the accompanying brief's demand to "build P1–P11" and "implement Reveal / motion.ts / verify tokens" asks for what the deck itself marks BUILT. <br>**Slide 02 settles the easing question for good: the deck prints no cubic-beziers at all.** Its six patterns carry durations and transforms only — Rise `translateY(16px)` 320ms, Stagger 60–80ms cap 6, Draw `stroke-dashoffset 100→0` 800ms, Cross-fade `opacity + 8px slide` 200ms, Lift `translateY(-2px) e1→e2` 120ms, Sweep `scaleX origin left` 480ms — **every one matching what is built.** The brief's `--ease-out-quint: cubic-bezier(.13,.27,.63,1)` is the **third different invented value** for that token across three briefs (after `(0.13,0.92,0.38,0.97)` and the real `(0.16,1,0.3,1)`). It has no source in the deck. <br>**Genuinely new, and it needs a decision: the azure ground.** Slide 01 reads *"Dark ground. Light on the line."* with the subtitle *"Every built beat, its choreography and its redlines — **on the azure ground the site is moving to**."* That is a stated intent to move the site to a dark azure surface, which **contradicts CLAUDE.md rule 10** — light mode only, no dark theme, "do not reintroduce a dark variant without changing this rule first". The deck is itself rendered dark. Not actioned: rule 10 is binding until it is deliberately rewritten, and this is a whole-site visual direction, not a motion change. <br>**Slide 07 confirms parallax is still the one unbuilt pattern** — "specified, not shipped", `±8px max`, `(hover: hover)` and `(pointer: fine)`, "never on touch", `STATUS not built`. It targets the *hero composition*, whose layered form no longer exists since the 2026-08-31 full-bleed rebuild, so it has the same composition problem as P3/P6/P7. <br>**Slide 08 "INTERACTION STATES" was the one slide with buildable new work, and three of its five items were missing.** Built today: **button 150ms → `--duration-press` 100ms** (the 1px lift was already right, only the speed drifted); **accordion `grid-template-rows 0fr → 1fr`** at 200ms, applied to all three accordions (homepage FAQ, `/faq`, `/pricing`) — the deck's and docs/09 §4's specified technique, unbuilt until now; and a **sliding tab indicator at 240ms** on the ClusterSwitcher via framer `layoutId`, which animates the existing filled pill between tabs rather than redrawing them as the deck's underline — a mock is not a mandate to change the design. Link underline (120ms) and focus ring (2px, offset 2px) were already exact. Two tokens added: `--duration-press`, `--duration-indicator`. <br>Verified: typecheck, build (48 routes), 48/48 contrast, 42 content routes; button measured `0.1s`; accordion `0px → 88.25px` with a `0.2s` transition; indicator slid `427 → 712` landing 1px from the selected tab. |
| 2026-09-01 | **M5 axe gate PASSED — 559 page loads, 13 viewports, zero findings from any motion work.** The gate the owner set before Stage 1 could ship. Covers Stages 0–2: the retuned rise token across all ~48 routes, the ChainRail draw/transit, the container-level stagger conversion (including `<Reveal as="ul">` and the `ul > li` restructuring, which was the change most likely to regress), `.lift`, `.cross-fade` and the P5 sweep. **Nothing on Chain, Platform, Capabilities, Intelligence or the list restructuring.** <br>**One finding, and it is pre-existing and unrelated:** `/security` at 640px, `[serious] color-contrast` on `.border-[var(--color-warning)]/35` — the "In progress" status pill. Already diagnosed on 2026-08-13: `--color-warning` (#e38f00) measures **2.29:1** on its own 12% tint and **2.56:1 on plain white**, so it fails as text anywhere, and `check-contrast.mjs` **defines `warning` on line 144 but never uses it in a pair** — it asserts `danger on bg` and has no `warning on bg`, so the token is loaded and silently never checked. The recommended fix remains the role-split §2.5 already made for cyan (keep the bright value for fill/border, add a dark one for text) rather than dragging amber from L 0.72 to 0.50 and turning it brown. Untouched here: it is a semantic-palette decision on a page this workstream did not modify. <br>A confirmation run was queued against the final build afterwards, since the gate run predated the token sweep and the Intelligence stagger — both attribute/class-only changes that axe cannot flag, but the earlier assumption-versus-measurement lesson applies. |
| 2026-09-01 | **M5 token discipline swept — and it removed a latent sticky-hover bug on touch.** Rule 8 says every duration comes from a token; thirteen call sites still carried `duration-200` on hover transforms. All chevron rotates and arrow nudges now use **`--duration-lift`**, making it the site's single hover-response duration — cards, chevrons and arrows all answer the pointer at 120ms instead of a mix of 120 and 200. The sticky header's background swap moved to `--duration-cross-fade` (200ms), which is what that state change actually is. No new token was invented for this. <br>**The bug it surfaced:** two card grids (`/resources/guides`, `/solutions`) hand-rolled the lift as `hover:-translate-y-0.5 hover:shadow-[…]` **outside any `@media (hover: hover)` guard**, so on a touch device `:hover` resolves on tap and the card stays lifted until you tap elsewhere. Replacing them with `.lift` — which is guarded — fixes it. Verified by tapping: transform is `none` after a tap at 390px, and `hover: hover` correctly reports false. <br>Verified: typecheck, build (48 routes), 48/48 contrast, card lift and chevrons both measured at `0.12s`, hover yields exactly `translateY(-2px)`. |
| 2026-09-01 | **M5 Stage 5 assessed — P3 and P6 are not buildable as specified, and a new asset defect found.** <br>**P6 Developers: all three redlines resolve to "already there" or "must not".** `CHROME rise 320ms` is **already satisfied by the asset** — the video has a framed terminal painted into it, tabs and all. A title bar was built and reverted within the hour because the screenshot showed **two nested windows**; `site/illustration.tsx` carries a `chrome={false}` prop for exactly this case. Reading the screenshot caught it, which is the repo's own standing lesson. `ROWS 220ms · 180ms apart` has **no DOM rows** — the code is pixels inside an mp4, so staggering them means replacing the video, an asset decision rather than a motion one. `OUTPUT cross-fade 200ms` shows `invoice.created  ok 142ms`; **that latency is invented and nothing measures it**, so it stays in the spec and off the page (rule 1 has no decorative exemption). <br>**New defect, and worse in kind than B10–B12.** While checking P6 I extracted a frame: the video types `process.env.ALGORYQ_ONE_ONE_KEY`, while the real sample in `developers.tsx` is `ALGORYQ_ONE_KEY`. The others are cosmetic misspellings; this is a **wrong environment-variable name in the developer-facing section, and it is copy-pasteable**. <br>**P3 Problem: the overlays would duplicate what is already on screen.** The redline is callouts naming the gaps *over* the video, 500ms apart, `timeupdate`-driven. §3 already names the four gaps as text in the adjacent column, so overlaying them would state the section's one idea twice **simultaneously** — a stronger version of the duplication that removed `home.jpg`. No other source content exists for callouts, and inventing copy to satisfy a motion spec is the wrong direction. The applicable part, `LIST 70ms stagger`, is built and now runs on one observer. <br>**The systemic finding, stated once.** Five of the seven choreographed beats specify motion for a composition the built site does not have: P2's labelled rail, P4's six module cards, P7's KPI chips and bars, P6's DOM code rows, P3's video overlays. The **vocabulary transferred cleanly** — Rise, Stagger, Draw, Cross-fade, Lift and Sweep are all built and measured against the deck. What remains is not animation work but one decision: whether these sections adopt the deck's compositions. That single decision covers P3, P6 and P7 together. |
| 2026-09-01 | **M5 Intelligence (P7) — the stagger built, the halo stopped on purpose.** The five points now stagger at 70ms off the `Reveal` that already wrapped that column, so it added **zero observers**; the screenshot rises with the same Reveal, which is P7's "static base". <br>**The halo was not built, and the CSS-vs-SVG decision is not what blocked it.** P7's redlines assume the slide's composition — three KPI chips (MARGIN/UTILISATION/DSO), a seven-bar chart, a `peak week 34` annotation. §7 has none of those in the DOM: it is one raster screenshot plus a bullet list, so `HALO 320ms, one KPI only` has no element to attach to and `BARS scaleY` has no bars. That is the third beat whose deck composition does not exist in the built section (after P2 and P4). <br>**The stronger reason: the halo exists to send the eye to one metric, and this asset carries blocker B11.** Its x-axis reads `Jan 1, Jan 13, Jan 9, Jan 16, Jan 22, Jan 25, Jan 27, Jan 29, Jan 33` — out of sequence, with a date that does not exist — and the member table repeats one row five times. Animating attention onto a defective chart is worse than leaving it still. Needs either a re-cut screenshot, or the deck's composition built natively as DOM (which would make every P7 redline applicable and remove the raster's baked-in defects, but supersedes the 2026-08-10 static-screenshot decision). <br>**Errors in the supplied Stage-by-stage brief, not applied.** Its "TOKENS REFERENCE" gives `--ease-out-quint: cubic-bezier(0.13, 0.92, 0.38, 0.97)` and `--ease-in-out-quint: cubic-bezier(0.83, 0.02, 0.17, 0.98)`; **both are wrong** — the real values are `(0.16, 1, 0.3, 1)` and `(0.65, 0, 0.35, 1)` — and the brief instructs "tokens verified" against that block, which would have corrupted correct values. It also re-proposed the already-rejected `stroke-dasharray: 520` and `--token-end-x: 420px`, six chain nodes where there are five, emoji icons, hardcoded copy in JSX, a nested `<Reveal>` per child (the very observer violation Stage 2 removed), `[data-reveal="true"]` (an attribute this codebase does not use), "26 contrast pairs" (48), and an **infinite 2s pulsing halo** animating `box-shadow` — which contradicts the deck's `320ms, one KPI only`, animates a property its own debugging guide forbids, and hardcodes `rgba(0,190,199,0.4)` instead of the cyan token. <br>Verified: typecheck, build (48 routes), five staggered lists measured at 70/70/80/70/70 ms, 48/48 contrast, 42 content routes, reduced motion 0/1025 faded. |
| 2026-09-01 | **M5 Stage 2 — Platform (P4) and Capabilities (P5), and a rule the whole site was breaking.** <br>**The find: P4's `OBSERVER: one, on the grid` was violated everywhere.** Stagger was implemented as one `<Reveal>` per child, so every grid carried one IntersectionObserver *per item* — four on the capability grid, seven across Problem, three on Security. Same visual result, N times the observers, and it contradicted the one line in the deck that talks about cost. Generalised the container-level approach first used for ChainRail's nodes into a shared `[data-rise-item]` pattern: one `<Reveal>` on the group, children delayed in CSS off the `.reveal-seen` marker. **Homepage observers 39 → 32.** Converted Capabilities, Platform, Problem (both lists) and Security. <br>**`<Reveal>` gained `as="ul"`, and it is required rather than convenient:** a wrapper `<div>` between `<ul>` and `<li>` breaks the list relationship, and the items stop being announced as a list of N. Verified all four staggered lists still render `ul > li` with 4/3/4/3 direct children. <br>**P5's sweep built** — `[data-sweep]`, 480ms `scaleX` from origin-left in `--color-brand-600`, the "one claim" under the four proofs. 1px tall so it animates as a transform and never a width; measured 1344px under the grid. **`.lift` added to Platform's app cards**, which were static boxes. **ClusterSwitcher reconciled** from 180ms with framer's generic `easeOut` to **200ms `[0.16, 1, 0.3, 1]`**, matching the deck's Cross-fade; it stays a keyed remount so it does not use the `.cross-fade` class — that needs both panels in the DOM, and an AnimatePresence version was already abandoned here over a React 19 exit-callback bug. <br>**`STAGGER_CAP` bit for the first time.** Platform's shared spine is eight chips against a cap of six, so measured delays are `0/70/140/210/280/350/350/350` — `tenant`, `workflow` and `reporting` arrive with `search` rather than trailing to 490ms. Every list until now was under six items, so the cap had changed nothing; this is the rule visibly working. <br>**Composition note:** P4's slide shows a 3×2 grid of six module cards, which does not exist in §4 — the section has a six-tab switcher, four app cards and eight spine chips. The *motion* redlines were applied to the grids that do exist rather than inventing the deck's composition, same judgement as P2. <br>Verified: typecheck, build (48 routes), `rise-item 0.32s`, capabilities `0/80/160/240ms`, sweep `0.48s`, 48/48 contrast, 42 content routes, 48 links, reduced motion 0/1025 faded with every animation `none`. **Axe audit re-queued against the Stage 2 build** — the earlier run was killed at 5/13 viewports because Stage 2 restructured DOM (list nesting, new elements), which is exactly the class of change an audit of the older build would not have covered. |
| 2026-09-01 | **M5 Stage 1 — the chain transit shipped: P2's signature moment, and the first Draw on the site.** `ChainRail` (`components/diagrams/chain-steps.tsx`) rendered by §2 beneath the card row, xl only. Deck slide 04, exactly: rail draws **800ms** `--ease-out-quint`, five nodes stagger **70ms** apart, cyan token travels **1400ms** `--ease-in-out-quint` from +500ms and **parks on the last stage**. Full write-up `09` §5.1. <br>**No client island and no fourth observer.** The animations are gated on `.reveal-seen`, a new stable in-view marker that `<Reveal>` now sets whenever `shown` is — including the two cases where `armed` is false and no rise runs (reduced motion, and a block already on screen at mount). Keying off `.reveal-in` would have reintroduced the exact false negative the three-path resolve exists to prevent, one level down: a visitor arriving via `/#chain` would never have got it. <br>**The default state is the finished state**, and only the marker winds it back. Written the other way round, the rail would be invisible whenever the marker never arrives — JS disabled, or a hydration failure. An entrance animation must not be able to hide content (rule 6). <br>**Three things went differently from the plan, each a correction worth keeping.** (1) **Nothing needed measuring.** `pathLength="100"` normalises the path, so the planned `getTotalLength()` step and the deck's `520` were both unnecessary — and `100→0` is what slide 02 specifies anyway. Editing `d` can no longer break the draw. (2) **The rail could not go behind the cards.** `ChainCurrent` already holds that band and is deliberately transparent across the middle, so a token running along it would vanish for two thirds of its journey — the one part that must be seen. It went below the row. (3) **Evenly spacing the nodes across the container was wrong**: the connector slot sits *after* each card, so card centres are not at 10/30/50/70/90%, and the rail visibly drifted from the row it describes. Fixed structurally with a CSS inset to the first and last card centres — `ms-[calc(10%-1.375rem)]` / `me-[calc(10%+1.375rem)]`, asymmetric because the connector is after the card. <br>**The token moves by an SVG transform in user units, never CSS pixels.** A fixed `translateX(420px)` would land on the rail at exactly one viewport width. Measured **0px** from the last node at 1280, 1440, 1728 and 2560, with the token staying a true circle (18.8×18.8) because `preserveAspectRatio` is left uniform — `none` would have stretched it and the node dots into ellipses. <br>**Errors in the supplied Stage 1 brief, not coded.** It specified **six** nodes named Request/WBS/Task/Timesheet/Invoice/Paid; the content module has **five** (Deal, Project, Plan, Time, Invoice) and deck slide 04 shows five, so its timeline ended at 420ms where the real one ends at 280ms. Its markup also hardcoded an `<h2>` and emoji icons — copy belongs in `content/`, and emoji are not this site's icon system. Its `translateX(420px)` and hard-coded `stroke-dasharray: 520` are the two techniques described above as wrong. <br>**Unlabelled by design:** the cards already name the five stages, and repeating them would state the section's one idea twice in a row — which is what removed `home.jpg` from this same section on 2026-08-10. `role="presentation"`, `aria-hidden`, nothing focusable, zero text nodes, so the rail adds no information that is not already an `<h3>`. <br>Verified: typecheck, build (48 routes), alignment and landing at four widths, reduced-motion end state, and absent below xl. **Axe audit running at time of writing** — it gates both Stage 0 and Stage 1 and had not returned. |
| 2026-09-01 | **M5 motion system — video analysed, spec extracted, Stage 0 (shared foundation) built and verified.** <br>**The reference video is not reference footage.** `Motion System Video.mp4` (1280×720, 30fps, 56.5s, no audio, untracked, in `~/Downloads`) is a **purpose-built motion specification deck for this site** — headed `ALGORYQ ONE · MOTION CHOREOGRAPHY`, tagged M5, eleven slides, each carrying written redlines: durations, offsets, easings, token names, asset names. So Phase 1 was a *transcription*, not an estimation exercise; the values are exact rather than ±50ms. Full extraction, slide by slide, with frame evidence and the method: `ANIMATION_SPEC_FROM_VIDEO.md`. Beat-by-beat mapping, drift tables and build order: `ANIMATION_IMPLEMENTATION_MATRIX.md`. <br>**Scope correction from the source itself: the hero is out.** Slide 01 reads "Every **non-hero** beat"; slide 03 is `P1 / HERO — LOCKED · STATUS: reference only`. The brief asked for all 11 sections; the deck excludes one. Ten beats in scope, and slide 11's beat order matches `page.tsx` exactly once the content-gated Testimonials is discounted — no resequencing implied. <br>**Decision (owner, 2026-09-01): `--duration-rise` 480ms → 320ms, site-wide, deck is authority.** The deck specifies Rise at 320ms and titles that slide "the rhythm everything inherits", which is an explicit claim to global authority. `--duration-rise` backs `.reveal-in`, so this reaches every `Reveal` on all ~48 routes — a 33% speed-up of every entrance on the site, not a homepage change. The rejected alternatives: keeping 480ms and demoting the deck to reference (which makes it unquotable and turns every future section into a judgement call), and 320ms on the homepage only (two rise speeds on one site — the shared page template would visibly differ from the homepage on the same scroll gesture). <br>**Decision (owner, 2026-09-01): the P7 KPI halo is CSS, not the deck's named `kpi-highlight.svg`.** A radial-gradient on a positioned element reads `--color-cyan-500` directly so it cannot drift from the palette, needs no request, and scales at any size. Recorded as a deliberate deviation from a named deck deliverable; Stage 4 is unblocked by it. <br>**Stage 0 shipped:** six new duration tokens (`--duration-cross-fade` 200ms, `--duration-lift` 120ms, `--duration-draw` 800ms, `--duration-sweep` 480ms, `--duration-transit` 1400ms) plus `--stagger-tight` 70ms / `--stagger-wide` 80ms; `.lift` and `.cross-fade` as shared classes; observer threshold 0.2 → **0.25** (P5 is the only slide stating one); and `src/components/site/motion.ts` carrying `STAGGER_CAP = 6` with a capped `stagger()` helper. Stagger drift reconciled — the site was running 50/60/70/80/**260**ms and now runs only multiples of 70/80, verified in the browser as 70/80/140/160/210/240/280ms with no literal delays left in any component. The `cap 6` rule is **latent** (every list is under six items) and was enforced precisely because it is: nothing would have told the person who adds a seventh card. <br>**Two traps in the brief, both avoided.** (1) Its "Current System" token block — `--dur-fast`, `--dur-base`, `--dur-slow`, `--ease-out`, `--ease-in`, `--ease-inout` — **describes tokens that do not exist in this repo**. An invalid `var()` is silently dropped by CSS, so coding against it would have produced animations that quietly do nothing on every section, with a green build. Real names are `--duration-*` and `--ease-*-quint`. (2) Its offered `Reveal` rewrite has a single IntersectionObserver; the shipped component deliberately has **three** resolution paths because a one-path version left 37 blocks at `opacity: 0` on `/#chain` (2026-08-11). `Reveal` was extended, not replaced. <br>**One real bug found by the build, not by typecheck:** `stagger()` was first exported from `reveal.tsx`, which opens with `'use client'` — that makes even a pure function a client binding, and every section here is a server component, so prerender failed with "Attempted to call stagger() from the server". Moved to a server-safe `motion.ts`. Worth remembering: `tsc` cannot see this class of error. <br>Verified: typecheck, build (48 routes), 48/48 contrast pairs, 42 content routes, 48 links, and in-browser — rise applies at 0.32s to opacity+transform, `.lift` at 0.12s giving exactly `translateY(-2px)`, no sticky hover on touch (`hover: hover` correctly false), and under `prefers-reduced-motion` **0 of 1008 elements faded** with `.lift` keeping only its shadow step. **Not yet run: the full 13-viewport axe audit** (~35 min) — it should run before this is called shippable. <br>**Remaining:** stages 1–5 in the matrix. Stage 1 is Chain (P2), the deck's declared signature moment — 800ms draw + 70ms nodes + a 1400ms ease-in-out token travelling the rail, the only ease-in-out and the only value above 900ms in the whole deck. Draw is **not implemented anywhere on the site today**. |
| 2026-08-31 | **Hero follow-up: flat scrim, navbar merged into the video, `hero-loop` moved again — §2 → §4, full width.** Three fixes on top of the same-day rebuild below. <br>**The scrim is flat now, and the gradient was the bug.** Reported as "the text sometimes hides in the video". It did, and the measurements that said otherwise were not wrong — they were the wrong *shape* of measurement. A gradient guarantees contrast at the points you sample; a 30-second hero is moving picture, so a bright highlight only has to drift into the light end of the ramp to take the copy with it. Re-tuning stops cannot fix that: every ramp has a light end. Replaced with one flat `color-mix(--color-band 74%)` — **no gradient, no shadow, no glow behind the text**, as asked. Because it is flat the worst case is now a single number that holds everywhere: measured on the rendered page at the **brightest pixel of the entire video**, `--color-band-fg` **7.89:1** and `--color-brand-300` **4.92:1**, *identical* for the nav strip and the copy column at both 1280 and 1920. Sampling method also corrected from a 95th-percentile crop of the copy region to whole-frame max, since flat means one number has to cover every pixel. `02` §2.13 rewritten around this. <br>**Navbar merged into the video.** `OVERLAY_HEADER_ROUTES` in `header.tsx` — a list, not a `pathname === '/'` test, because the condition is "this page has a video hero". At the top of such a route the header renders fully transparent with no background of its own (a second veil there would darken the nav strip relative to the hero and put a seam across the join, which is the thing being removed) and switches its logo, nav links, hamburger and Sign-in button to band tokens. The hero pulls itself up by the new `--header-h` token so the footage runs behind it, and pads the same amount back so nothing lands under the navbar — one token, so header and hero cannot disagree. On first scroll the existing `scrolled` state takes over and the normal solid header returns: past the fold it sits over light sections where light nav links would be unreadable, and reusing `scrolled` keeps one trigger instead of a second observer that could disagree with it. `Button` gained a `ghostOnBand` variant — plain `ghost` is `--color-fg-muted`, ~3:1 over the scrim. Verified in all four states: transparent at top, solid scrolled, transparent again on return, and untouched on `/pricing`. <br>**`hero-loop` moved §2 → §4 and rendered full width**, component renamed `ChainVideo` → `PlatformVideo` to match. §2 was the wrong home: it is a *sequence* (Deal → Project → Plan → Time → Invoice) and the loop draws a *hub* — one portal, seven modules wired to it, one feed carrying all their events. That is §4's thesis verbatim ("all of them sit on one gateway and one shared spine"), so it now opens §4 at full container width instead of as a 30rem thumbnail. The architecture diagram still closes §4 with the ClusterSwitcher between them, so "what it looks like" and "how it is built" never read as one restating the other (rule 9). Its text equivalent and Demo-tenant disclosure travelled with it again. <br>**⚠ Full width makes blocker B10 unmissable.** At 30rem it was a small thumbnail; at 1342px the misspellings in the baked-in interface are headline-sized, and one of them is **the product's own domain and brand name** — `partal.one.algerys.con/actions` against the real `portal.one.algoryq.com` — alongside "Orhit Health", "Awalling finance approvai" and "Alies CRM Roliout". This is not a new defect, it is B10 at four times the size, on the homepage. **The fix already exists in the repo:** `components/diagrams/integration-web.tsx` renders the same picture from `content/homepage.ts`, so every string is data and correct by construction; it was kept precisely as this fallback. Swapping it in is a one-line change and is recommended. <br>**Also fixed: a verification hazard that produced fake failures.** `check:links` reported two 404s on `/_next/...` chunks. Neither was real — the dev server left running on :3500 writes to the same `.next` directory a production build is verified from, and had replaced the hashed chunks the built HTML pointed at. `next.config.mjs` now honours `NEXT_DIST_DIR` (default `.next`, so CI and deploys are unchanged) and verification builds into `.next-verify`, gitignored. Re-run isolated: 48 links, 42 content routes, both clean. <br>Verified: typecheck, build (48 routes), 48/48 contrast pairs, no horizontal overflow at 390/768/1280/1920, reduced motion still falls back to the hero poster and never starts the §4 loop, and all 8 header tab stops keyboard-reachable at 33–40px with focus rings. |
| 2026-08-31 | **Hero rebuilt as a full-bleed video; `hero-loop` moved to §2.** §1 was copy-left / boxed-video-right on a light `.bg-aurora` wash. The new office/dashboard montage is now the section's background — `absolute inset-0 object-cover`, no card, no radius, no max-width — with the copy on a dark scrim above it. <br>**The white halo was the light wash, and it is gone at the source.** `.bg-aurora` and `.bg-grid` were removed **from §1 only**: a light radial gradient behind a boxed video is what haloed its edges, and with the video full-bleed there was nothing left for a light background to do except bleed. Both utilities are untouched at their four other call sites (CtaBand, FinalCta, PageHero, 404). `.bg-grid` was removed rather than dimmed — it was drawn for a plain surface, reads as moiré over footage, and its radial mask fights the scrim's falloff; there was no opacity at which it added anything. <br>**The scrim's opacity was measured, and the first attempt was wrong in an instructive way.** Footage has no single background colour, so the ratio was computed against the frames: 60 samples at 2fps, cropped to the region the copy occupies, composited against `--color-band` and taken at the 95th percentile. The first pass composited in **linear light** and concluded it needed α≈0.88 — a veil so heavy it would have hidden the video the change exists to show. Browsers composite alpha in **gamma-encoded sRGB**, which is materially darker for the same alpha; redone correctly the floor is **0.72**. Verified on the rendered page, not just in theory: **`--color-band-fg` 8.4–8.8:1** (clears §2.11's ≥7:1 body rule) and **`--color-brand-300` 5.2–5.5:1** (clears ≥4.5:1 for headlines). Two tokens were measured and rejected — `--color-brand-600`, the accent on light surfaces, is **2.3:1** over the scrim, and `--color-brand-400`, the closer match to the requested colour, **2.95:1**. `--color-band-fg-muted` is **3.0:1** over video and is now documented as unavailable there, so the subhead reads as secondary through size and weight instead of opacity. Codified as `02` §2.13 with the method, because a new cut is a new background and none of these numbers transfer. <br>**`hero-loop` was relocated, not retired** — it draws Deal → Project → Plan → Time → Invoice, which is §2's own subject, so it now sits beside that section's intro capped at 30rem against the card row's full width (a supporting visual, not a second centrepiece). Its `sr-only` text equivalent and its Demo-tenant sample-data disclosure travelled with it; dropping either would have been a rule 1/5 regression. It plays **on view** via IntersectionObserver rather than autoplaying from load — a loop that has been running since page load is mid-cycle and mid-buffer when you reach it, which is exactly what reads as choppy — with `preload="metadata"` and a reserved aspect box. Verified: paused/t=0 off-screen, playing in view, paused on leave, **box height constant at 408px across all three states**, and never playing under reduced motion. <br>**Assets:** master re-encoded from 18.6MB → **VP9 3.1MB + H.264 5.1MB**, audio stripped (it is muted, so the track was pure weight), both native 1920×1080, never upscaled. VP9 order is earned, not assumed — at CRF 37 it came out *larger* than H.264 (5.53 vs 5.14MB) and was rejected, since a browser takes the first playable `<source>` and that would have served Chrome the worse file; CRF 44 lands at 3.1MB with the least-veiled right edge checked against the master for blocking. The master's filename contained a literal U+2026 ellipsis and sat at `public/` root — renamed ASCII and moved to `public/media/hero-office-master.mp4`. <br>**⚠ New defect, same class as B10/B11/B12 — logged, not hidden.** The footage's baked-in interface text is garbled: "Milsstone", "Fanding", "Bh Logged", "Inselies", "Frer 3, 2023", "Sarah Samith", and a nonsense URL. Full bleed at 1920px makes it far more legible than the old boxed treatment did. It ships at the owner's explicit instruction, with a rule 1 disclosure in the copy stating the interface is illustrative. **The clean remedy is a trim, and it is cheap:** the garbled UI is confined to **0–7s and 20–22s** of the 30s master — a loop cut from **23–30s**, or **9–19s + 23–30s**, is all human footage and carries none of it. <br>Verified: typecheck, build (48 routes), 48/48 contrast pairs, 42 content routes, 48 links, no horizontal overflow at any width, reduced motion falls back to the poster on the hero and never starts the §2 loop, and hero CTAs keyboard-reachable at 52–54px with focus rings. |
| 2026-08-13 | **Whitespace pass — the two section-rhythm clamps retuned, and every off-scale gap in the section components brought onto the scale.** A cross-cutting spacing refinement over built work (M0+M1), not a numbered doc. **Measured first, in a browser, against the production build** — the source alone cannot show this, because the offender is paid *twice* at every boundary (bottom padding of section N + top padding of section N+1) and so is invisible in any single file. Ranked at 375/768/1280/1920 across the homepage and four sub-pages: **(a) section padding was 24.3% of the homepage's entire scroll height at 1920px** and 20.2% at 1280 — boundary gaps of 256px at 1280 and **336px at 1920** — against (b) heading-to-content gaps of 56–64px, (c) card-grid gaps of 24–40px, and (d) Container padding, which is not an offender at all (`max-w` governs at ≥1280, so the 48px gutter never binds there). So (a) it was, and it was fixed at the root: `.section-y` `clamp(5rem, 9vw, 9rem)` → **`clamp(3.5rem, 6vw, 6rem)`** and `.section-y-lg` `clamp(6rem, 11vw, 12rem)` → **`clamp(4.5rem, 8vw, 8rem)`** in `globals.css`, which propagates through `<Section>` to every homepage section and all ~40 template routes from two edited values. Both stay fluid clamps (the responsive contract in `02` §4), `lg` stays the rarer roomier one (ceiling ratio unchanged at 1.333), and both now leave their floor at ~900px and hit their ceiling at 1600px so the pair scales over one window. Result: homepage **11,019px → 10,010px at 1280** and **11,832px → 10,468px at 1920** (−9% and −12% of total scroll), boundary gaps −30%, with the tint/band tone rhythm and the deliberate `size="lg"` pivots (Hero, Chain, Permissions, Security, FinalCta) fully intact — hierarchy was *not* flattened. **The internal-gap half turned out to be the same edit as a conformance fix:** every value tightened — `gap-14`, `gap-7`, `mt-14` — was already **off** the 4px scale `02` §4 documents (which has 12 and 16, but no 14; 6 and 8, but no 7), and they were also the loosest values in the components, so tightening and conforming coincided exactly. `gap-14`→`gap-12` (8 files), `gap-7`→`gap-6` (3), `mt-14`→`mt-12`, stacked two-column splits `gap-12`→`gap-10` (6 files, `lg:gap-16` kept — at `lg` that is a column gutter, not vertical rhythm). Footer trimmed `py-16 lg:py-20`→`py-12 lg:py-16` (it sits outside the token system, so step 2 could not reach it); header's `h-16` bar left alone as already correct. **`PageBlock`'s own `py` was deliberately not touched** — the 2026-08-11 "padding follows content" pass derived those values from measured content-to-padding ratios, and re-cutting them by feel would have discarded that evidence. Verified: typecheck, build (48 routes), 48/48 contrast pairs, the full **43 routes × 13 viewports axe audit with zero findings**, before/after screenshots at all four widths plus 1:1 boundary crops at 375 and 1920 confirming nothing collapsed or overlapped, and reduced-motion re-checked the strict way — under `prefers-reduced-motion: reduce` **0 of 1002 elements** are faded or shifted without scrolling (38 are armed without it), with 57–75 keyboard stops per route all sized and all showing a focus ring. `02` §4 rewritten with a measured value table; its previous entry read `clamp(5rem, 10vw, 12rem)`, **a single value matching neither token as shipped**, so the doc was already drifting from the code (rule 3 applies to docs describing code). `02` §2.10 now says explicitly that it covers the *tonal* half of section rhythm and points at §4 for the vertical half — the two share a name and read as competing specs otherwise. <br>**A latent flake in `audit.mjs` surfaced and was fixed rather than waved through.** The first full run after the change reported two findings — a *serious* colour-contrast violation on the chain eyebrow, and a 24px-tall `/roi` slider called undersized at exactly the 24px minimum. Both were artifacts of the same thing: the script scrolled to trigger the reveals, then settled **600ms** — but `Reveal` resolves through three paths whose slowest is a **2000ms failsafe**, so any block whose observer missed during the fast scroll was still at `opacity: 0`, its 480ms rise not yet started, when axe sampled it and blended it against its background. Proof it was not real: the finding **moved between viewports run to run** (375, then 320; `.reveal` then `.reveal-in`), and with a 4s settle the homepage reports **0 colour-contrast violations at 320 in both motion modes**, with that eyebrow measured at `opacity: 1` on its documented token pair. The settle is now **2600ms** (2000 failsafe + 480 rise + headroom) and the full run is clean again. This was latent long before this pass — it fired now only because a shorter page changed the scroll geometry. A gate that reports a real-looking a11y failure depending on timing trains you to ignore it, which is worse than no gate. |
| 2026-07-31 | Workstream opened. Codebase audited (30 services, 254 models, 4 apps, 65 UI components verified by enumeration). `website/` created with CLAUDE.md + docs 00–17. Implementation not started. |
| 2026-07-31 | Figma file supplied and analysed. Scope changed to build-from-scratch in `website/`; theme fixed to light-only. M0 + M1 built: self-contained Next 16 app, light-only oklch token system, typed content modules, header/footer chrome, 20-section homepage with three interactive islands and two diagrams. Typecheck and build green; rendered HTML verified for content and for the no-fabricated-proof rule. |
| 2026-07-31 | **First real browser audit, and the fixes it forced.** Added `scripts/audit.mjs` (Playwright + axe over every route × 13 viewports, checking overflow, a11y, tap targets, sub-12px text and console errors) and `scripts/check-contrast.mjs` (oklch → sRGB → real WCAG ratios for the 26 pairs the site renders). First run: **207 findings** while the content and link checkers were green. Fixed: contrast — recomputed `--neutral-500`/`--neutral-600` and split `--color-success` into light and band variants because no single lightness clears 4.5:1 on both white and near-black; horizontal overflow at 390px from a negative-margin tablist bleed; `role="img"` wrapping six real links (nested-interactive); three keyboard-unreachable scroll regions; a 6px-tall range slider; and 10–11px text on 18 routes. Second run: **82 findings** → all 55 `max-w-[Nch]` became `max-w-[min(Nch,100%)]` (ch units ignore the container and overflowed at 320–480px), standalone inline links raised to a 24px target, and the audit itself was corrected to scroll before measuring — reveals sit at `opacity: 0` until observed, which axe correctly read as zero contrast. |
| 2026-07-31 | **Chain alignment pass across the deep pages.** Added a `ChainStrip` to the shared page template — a five-stage Deal→Project→Plan→Time→Invoice indicator with the page's own links highlighted, a one-line note on its role, and a link back to `/#chain`. It exists because most visitors land on a deep page from search rather than the homepage, so the positioning has to be re-established there rather than assumed. Rewrote the hero headline and intro on all 5 product pages to name their position in the chain, and wired chain context into `/platform/workflows`, `/platform/customization`, `/security/permissions`, `/solutions/professional-services`, `/solutions/agencies`, `/solutions/by-role/{sales,delivery,finance}`. SEO descriptions updated to match. Verified: 42 routes clean, 44 links clean, strip renders on all 10 target pages. |
| 2026-07-31 | **Repositioned to "deal → delivery → cash"** after the user chose it from four costed options. Rewrote doc 01 (positioning statement, wedge table with the cost of the choice stated, nine-beat spine, headline and section claims), rewrote `content/homepage.ts`, added a new `Chain` centrepiece section (five linked stages, each labelled with what survives the handoff), resequenced the homepage, and updated the site tagline and metadata. Permissions kept its full section but is now framed as "why the chain holds" rather than as the headline. Rebuilt and re-verified: 42 routes clean, 44 links clean. |
| 2026-08-10 | **Spacing / content audit, Phase 2 — all twelve work-plan items landed.** Spacing: the four blocks that bypassed `.section-y` with raw `py-*` (Hero, PageHero, FinalCta, CtaBand, plus 404) now use the fluid utility, which fixes a rhythm that *inverted* with width — the hero padded 96px against every section below it at 129.6px, and 96 against 144 at 2560; and it closes a 67% gap between `FinalCta` and `CtaBand`, the same closing ask rendered two different sizes depending on which page you were on. A single four-step gap scale replaced six competing values across twelve pages. `size="lg"`, previously used at exactly one call site site-wide, now marks the three structural pivots (Chain, Permissions, Security). Content: **all homepage copy moved out of JSX into `content/homepage.ts`** — seven sections held it inline, which is *why* four homepage headlines had drifted into being word-for-word identical with their own destination page, invisible to anyone reviewing `src/content/`. That move was verified the strict way: the rendered homepage HTML is byte-identical before and after, apart from Next's build hash. Then the duplication itself: three homepage headlines rewritten to tease rather than repeat (destinations untouched — they are canonical, rule 3); §13 Security cut from six control cards to three area teasers, since a visitor clicking "See the security controls" was landing on the same six cards under the same sentence; §8 Permissions cut from four bullets to two, dropping the pair that restated `/security/permissions`' own block titles, one verbatim but for a hyphen; §5 Thesis cut from six before/after rows to three, dropping the four that restated §4 Problem ~500px earlier. Three filler sentences cut, `devices.honesty` cut as a triplicate of `/platform/mobile`, `valueProp` dropped from the Architecture cluster cards so §7 stops re-pitching what §5 covers. Dead code deleted (`Card`, `CLUSTER_BY_ID`, three `DEMO_*` arrays); the `CONTACT` constant — which exists specifically to satisfy rule 12 and had **zero importers while seven files hardcoded the addresses it holds** — is now wired into the four JSX call sites. Four deviations from the plan as written, each recorded at the finding: A3's `Clusters → subtle` was **not applied** (the palette migration had already dissolved the run it targeted, and applying it would have created a subtle/subtle pair), `demo:67` was restructured rather than given `gap-14` (it is a heading cluster, not sibling blocks), and B7's `/company/about` caveat was **kept** rather than cut — its homepage twin died with `proof-band.tsx` on 2026-08-09, so it is now the site's only explanation for the absent logo wall, and cutting it would have removed an honesty statement rather than a repetition. Verified: typecheck, build (48 routes, zero services), 45 links, 42 content routes, 48/48 contrast pairs, and the full 13-viewport axe audit. |
| 2026-08-11 | **Scroll-jump reveal bug fixed — the real finding from a five-point UI/UX audit; two of the five did not reproduce.** <br>**Fixed (the serious one):** loading **`/#chain` left 37 blocks at `opacity: 0`** — and that is a URL the site ships itself, from the hero's "See the chain" button and the ChainStrip on every deep page. Any instant scroll change did the same (38 blocks), which is what back/forward scroll restoration and a fast flick both look like. Cause: `Reveal` decided once, at mount, whether a block was near the fold, using `rect.top < innerHeight * 0.6` — so it armed content that was *already visible* in the bottom 40% of the viewport, and on a hash navigation the mount happens at scroll 0, arming everything below and then depending entirely on one IntersectionObserver callback. `Reveal` and `CodeScan` now reveal via **three independent paths, any one sufficient** — observer, a passive scroll re-check, and a 2s failsafe — and the arming test widened to `rect.top < innerHeight`, so anything on screen renders unconditionally. **37/38 → 0 blank blocks** in all three scenarios; the JS-disabled baseline still ships 0 hidden elements. <br>**Fixed (minor):** the hero poster's `<img>` branch — what phones and reduced-motion users actually get — now goes through `next/image` and serves **AVIF at 640/750** instead of a raw 74KB JPEG at 1152px. The `<video poster>` attribute keeps its raw path and cannot be routed: `poster` is fetched by the media element, which does not participate in the optimizer's Accept-header negotiation. Four decorative SVG/icon nodes gained explicit `aria-hidden`. <br>**Did not reproduce — no code changed:** the audit reported the deal-to-cash chain appearing **three times** on the homepage, including a "~2,540px pinned/scroll-jacking walkthrough", and the **5th recap card clipped off-screen at 1440px**. Measured instead: the page contains **exactly 5 stage headings total** (one chain, not three); there is **no scroll-jacking anywhere in the codebase** — no `useScroll`, `whileInView`, ScrollTrigger, or pinning, and framer-motion appears only in the cluster switcher and permission matrix; and all five cards are **fully visible with zero overflow at 1280, 1440 and 1920** (at 1440 the last card ends at x=1392 inside a 1440 viewport, matching the container's 48px padding). The 2,477px figure is the section's **mobile** height at 390px, where the row correctly stacks to a column with down-arrows — which is almost certainly what was read as a step-by-step walkthrough and then double-counted against the desktop row. Deleting that section, as the audit proposed, would have removed the site's centrepiece and the chosen positioning wedge on the strength of a viewport mix-up. <br>**Known residual:** on small screens the browser fetches both the raw poster (from the SSR `<video poster>`) and the AVIF, because the component renders the video first and downgrades on mount. Removing the double fetch means changing the SSR default and re-testing CLS — worth doing, not done here. |
| 2026-08-10 | **`home.jpg` pulled back off the homepage — it duplicated the native chain grid.** Placed under §2's five stage cards earlier the same day and removed after review: the cards already draw Deal → Project → Plan → Time → Invoice, so the section stated its one idea twice in a row (rule 9). The grid is the better artefact and is not replaceable by a picture of itself — it carries the real service names (`sales-service` … `billing-service`), the per-stage plates, the connectors, and it is live DOM that reflows to a column on narrow screens. The file stays in `public/illustrations/` for a possible non-duplicate use. **No spacing fix was needed:** the container is `flex flex-col gap-14`, so removing a middle child left the remaining three evenly spaced — measured 56px / 56px at both 390 and 1440, and the section returned to exactly its pre-illustration height (1029px desktop, 2477px mobile). Grid verified intact: five cards, all five service labels, zero console errors. **A same-pattern audit of the other 17 placements found three more strong duplicates and four partials — listed for decision, not removed:** strong = `security.jpg` (draws the same six control areas the native grid renders directly below it), `pricing.jpg` (the same six cluster names as the `CAPABILITY_GROUPS` cards below it), `security-permissions.jpg` (the same five resolution stages, in the same order, as the native chain beside it); partial = `product-revenue.jpg` (4 of 6 chain labels), `platform-ai.jpg`, `security-compliance.jpg` (its DSR half restates a native chain in the next block), `platform-workflows.jpg`. |
| 2026-08-10 | **18 subsection illustrations placed across 14 routes.** New shared `components/site/illustration.tsx`: a browser-chrome card — the homepage video card's exact radius, border and `--shadow-e2`, plus a thin macOS-style title bar — wrapping a `next/image` in a `1376/768` box with `object-cover`. One component, eighteen call sites, so the frame cannot drift; **measured CLS 0.000 on every one**. Fifteen flow through data: `PageBlock` gained an optional `image`, rendered in the template's existing 5fr aside column, so each illustration lands beside its subsection's bullets and inherits the alternating left/right rhythm already on those pages. Three are bespoke — the homepage chain section, `/security` "Control areas", `/pricing` "What you can switch on" — and take a wider `sizes` because those layouts are single-column. Alt text is written per subsection, 156–247 characters, describing what each image actually depicts rather than "product illustration". **Two judgement calls:** the chrome dots are **neutral, not red/amber/green**, because the palette migration quarantined the semantic colours to functional use and a decorative traffic light is exactly the borrowed-semantics case that rule exists to stop; and `chrome={false}` on `platform-customization.jpg`, which already has window chrome painted into it. **No captions.** The obvious line — `SampleDataNote`'s "Sample data from the Algoryq One Demo tenant" — would be false on all eighteen: they are abstract illustrations, not renders of that fixture set. Verified: typecheck, build, 47 links, 42 content routes, all 18 confirmed present in the correct subsection by nearest-preceding-heading, and no stretching, overflow or broken loads at 390/768/1440. **Every video untouched** — re-confirmed 3 playing on the homepage at desktop, 2 below 1024px. **Nine of the eighteen have garbled text baked in — logged as blocker B12.** |
| 2026-08-10 | **Homepage §7 Reporting: the looping `GraphVideo` replaced with a static screenshot.** `public/images/reporting-utilisation-dashboard.jpg` (renamed from the supplied `watermark-removed-SaaS_analytics_dashboard_mockup_____202608102037.jpg` — note it is a **JPEG**, not the PNG it was described as). Rendered through `next/image` rather than a bare `<img>`: the source is 2752×1536 and `next.config.mjs` already requests AVIF/WebP, so this gets format negotiation and a responsive srcset, and the intrinsic width/height reserve the box — **measured CLS 0.0000** at 390/768/1440. The frame is the one the video had (rounded-xl, hairline border, `--shadow-e2`, `overflow-hidden`), minus `bg-black`, which existed to back a letterboxed video. `SampleDataNote` gained an optional `children` override so this caption could disclose honestly — the mockup is **not** the Demo tenant dataset, so the default "Sample data from the Algoryq One Demo tenant" line would have been a false provenance claim (rule 1). Scoped strictly to this section: the hero video is a different asset (`hero-loop.webm/mp4` + poster) and was re-confirmed playing, as were the Problem and Developers videos. Removing the island also makes §7 fully server-rendered. **`GraphVideo` and its 1.8MB `Workflow_lines_animating_on_dash….mp4` are now unreferenced but were deliberately left in place, not deleted** — the standing instruction is that videos are not removed from the repo. They ship in `public/` until someone decides otherwise. **The new image has data defects — logged as blocker B11.** |
| 2026-08-10 | **Consolidation calculator moved from the homepage to `/pricing`.** The homepage §10 ROI section is gone (12 → 11 rendered sections); the calculator now sits on `/pricing` between "What you can switch on" and the pricing FAQ, which is where the visitor already has a per-seat price in front of them and is doing the arithmetic anyway. Implemented as a sibling `Reveal` block, not a transplanted `Section` — `/pricing` keeps all its content inside one `Section` with a `gap-14` Container, so moving the homepage's `tone="tint"` wrapper across would have nested a Section inside a Section. **The `RoiTeaser` island is untouched**: same state, same sliders, same number inputs, same formula, same "Open the full calculator" link to `/roi`, which is still the richer version. Heading copy moved `homepage.ts roiIntro` → `pricing.ts CALCULATOR`. `Reveal` gained an optional `id` prop so the new block could carry `id="calculator"` with a matching `scroll-mt`. **No links needed updating** — the homepage section never had an anchor and nothing scrolled to it; every `/roi` reference on the site was already a full-page link and still resolves. Verified: typecheck, build, 47 URLs with **zero orphans**, 42 content routes, homepage down to 11 sections with tone still alternating on every boundary and no gap at the Security → FAQ junction, calculator arithmetic checked at three input sets plus keyboard slider operation (all correct), and **zero console errors on `/`, `/pricing` and `/roi`**. All four videos re-confirmed playing and looping. |
| 2026-08-10 | **Homepage restructured to a Zoho-style shape — 16 rendered sections → 12 — with every video kept.** The client asked for fewer sections, tighter headers and less text, and added one hard constraint: no video may be removed, replaced or lazy-stripped. Hero, Problem, Intelligence and Developers each host one, which made those four ineligible for merging away and set the realistic floor at 12 rather than the 9 in `homepage-section-reduction-proposal.md`. **Merged:** Thesis → Problem (the failures and their answer were one argument split across a section break and a video; the merge ran this direction, rather than Problem → Chain as proposed, precisely so `SystemArchVideo` kept its home); Clusters + Architecture → a new `Platform` section (both enumerated the same six clusters from the same array — audit B5 — so the ClusterSwitcher stays as the body and Architecture contributes only its layer diagram: four apps, one gateway, the shared spine); Devices → the fourth card in Capabilities. **Deleted:** the Solutions section — six links, no body copy. **Copy:** Problem's four `{title, body}` cards became four scannable lines, and the subs on hero, chain, platform, permissions, intelligence, developers, security and roi were cut hard (permissions alone went from 57 words to 29). **Dead code:** a 66-line unreferenced `ReportMiniature` in `intelligence.tsx` and an unused `CodeScan` import in `developers.tsx`. The two standalone fallbacks in `components/diagrams/` were **kept** — `integration-web.tsx` is the live workaround for blocker B10. **A real defect surfaced while cutting Solutions:** its §3.4 premise, that all six destinations were already in the mega-menu, was false. The menu listed five of eight, and a nav group with children renders as a `<button>`, so a group-level `href` is inert — **nothing on the site linked to `/solutions` at all**, leaving the index and `by-role/{hr,sales}` unreachable by crawl already, with `by-role/delivery` orphaned by this change. Fixed in the nav; the crawl went **45 → 48 URLs with zero orphans**, so removing a section left the site better connected than it found it. Verified: typecheck, build (48 routes, zero services), 48 links zero broken zero orphans, 42 content routes, 48/48 contrast, the 13-viewport axe audit, and a per-section pass at 390/768/1440 confirming 12 sections, no orphaned headers, no empty sections, no horizontal overflow, and all four videos present (three below 1024px — the hero's is deliberately gated to ≥1024px with a poster). |
| 2026-07-31 | M2–M4 built in the same session: shared page template, 5 product cluster pages, 6 platform pages, security hub + 3 deep pages, 4 developer pages, solutions index + 3 industries + 5 roles, pricing (live plan data, degradation path first), ROI, FAQ, demo, contact with a real API route, 3 company pages, 3 legal pages, 3 resources pages, 404, sitemap, robots, security headers and legacy redirects. Two verification scripts added and run: `check:links` (45 URLs, zero broken) and `check:content` (42 routes, zero failures, zero warnings). Build verified green with every service URL pointing at a closed port. |
